import { SafeAreaView, Text } from "react-native";

const DetailsScreen = () => {
  return (
    <SafeAreaView>
      <Text>Info screen</Text>
    </SafeAreaView>
  );
};

export default DetailsScreen;