import { SafeAreaView, ScrollView, Text, View } from "react-native";

const InfoScreen = () => {
  return (
    <View style={{backgroundColor: "white"}}>
        <ScrollView>
      <Text style={{fontSize: 100}}>Info screen</Text>
      <Text style={{fontSize: 100}}>Info screen</Text>
      <Text style={{fontSize: 100}}>Info screen</Text>
      <Text style={{fontSize: 100}}>Info screen</Text>
      </ScrollView>
    </View>
  );
};

export default InfoScreen;
